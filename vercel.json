{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/config", "dest": "server.js" },
    { "src": "/api/auth/login", "dest": "server.js" },
    { "src": "/(.*)", "dest": "public/$1" }
  ]
}
